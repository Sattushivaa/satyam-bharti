const $=(e)=>document.querySelectorAll(e);
let nav = $('#nav')[0];
let nav_btns = $('.nav_btn');
/*nav_btns.forEach((btn)=>{
  btn.addEventListener('click',(e)=>{
    hd._data_.__data__.head = btn.getElementsByTagName('span')[0].innerText;
  })
});
*/
